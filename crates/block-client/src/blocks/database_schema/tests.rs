use block::Block;
use uuid::Uuid;

use super::{DatabaseField, DatabaseFieldType, DatabaseSchema, DatabaseSchemaOperation};

mod renaming_field_preserves_its_uuid;
mod schema_adds_and_removes_fields_by_uuid;
