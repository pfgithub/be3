use super::*;

fn scale(value: u8) -> Scale {
    Scale::new(value).unwrap()
}

mod and3_challenge_outputs_three_input_and;
mod flip_one_bit_challenge_inverts_only_the_top_bit;
mod memory_cell_challenge_outputs_old_value_before_set;
mod two_tick_delay_challenge_outputs_input_from_two_ticks_ago;
